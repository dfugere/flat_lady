.header
  %h1 New imported_company

.body
  .section
    = error_messages_for :imported_company

    - form_for(:imported_company, :url => collection_url) do |f|
      = render :partial => "form", :locals => { :f => f }
      %p= submit_tag "Create"

    = link_to 'Back', collection_url